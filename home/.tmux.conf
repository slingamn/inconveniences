# make the control ("prefix") keychord Control-T, instead of Control-B
unbind C-b
set -g prefix C-t

# fix the status bar colors
set -g status-bg blue
set -g status-fg white

set -g mode-keys vi

# allow easy window renumbering
bind-key m movew
